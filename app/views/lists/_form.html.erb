<%= form_with(model: list, local: true) do |form| %>
  <div class="text-center">

    <div class="form-group">
      <%= form.label :name, 'Nome'  %>
      <%= form.text_field :name, class: 'form-control col-md-4 offset-md-4', required: true %>
    </div>

    <div class="form-group">
      <%= form.label :email %>
      <%= form.text_field :email, class: 'form-control col-md-4 offset-md-4', required: true  %>
    </div>

    <div class="form-group">
      <%= form.label :occupation_id %>
      <%= form.select :occupation_id, @occupations, { prompt: 'Selecione o Cargo' }, class: 'form-control col-md-4 offset-md-4', required: true %>
    </div>

    <div class="form-group">
      <%= form.label :salario, 'Salário' %>
      <%= form.text_field :salario, class: 'form-control col-md-4 offset-md-4', required: true  %>
    </div>


      <%= form.submit 'Cadastrar', class: 'btn btn-primary', data: {disable_with: 'Cadastrando...'} %>

  </div>
<% end %>
